apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  name: apps-tls-routes
  namespace: openshift-gitops
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
  source:
    path: tls-routes
    repoURL: https://github.com/jkeam/lab-lan-config.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
